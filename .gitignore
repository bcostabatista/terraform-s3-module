# Ignore Terraform state files, which contain sensitive information
terraform.tfstate
terraform.tfstate.*

# Ignore the Terraform configuration override file
terraform.tfvars    # Used for sensitive variable values
*.tfvars            # Include any other .tfvars files
*.tfvars.json

# Ignore crash logs generated when Terraform crashes
crash.log

# Ignore plan files, which are generated when running `terraform plan -out=`
*.tfplan

# Ignore the `.terraform` directory, which contains provider plugins and other local data
.terraform/
.terraform.lock.hcl

# Ignore backup files generated by Terraform when files are overwritten
*.tfstate.backup

# Ignore locally specific files (if applicable)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore sensitive provider or backend credentials
credentials.tfvars
*.auto.tfvars    # Use with caution; these files may contain sensitive values

# Ignore any personal files or editor-specific files
.idea/
.vscode/
.DS_Store
